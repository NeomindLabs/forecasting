# Forecasting

A Ruby gem to interact with the Forecast API.

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'forecasting'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install forecasting
